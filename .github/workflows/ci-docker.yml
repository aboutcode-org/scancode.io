name: Docker Images CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-20.04

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Build and run the docker-compose stack
        run: docker-compose up -d

      - name: Run tests
        run: docker-compose exec worker ./manage.py test --verbosity=2 --noinput
